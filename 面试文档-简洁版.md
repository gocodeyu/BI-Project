# BI智能数据分析平台 - 面试简洁版

## 项目概述

**项目名称**：BI智能数据分析平台  
**技术栈**：Spring Boot + MyBatis-Plus + Redis + RabbitMQ + Redisson + Caffeine + AI SDK  
**核心功能**：用户上传Excel文件，系统自动调用AI生成数据分析和可视化图表

---

## 核心技术亮点

### 1. 异步化处理（RabbitMQ）

**问题**：AI分析耗时长（10-30秒），同步调用会阻塞用户

**解决方案**：
- 使用 RabbitMQ 消息队列实现异步处理
- VIP队列（5-10个消费者）+ 普通队列（1-2个消费者）
- 手动ACK + 死信队列保证消息可靠性
- 自动重试机制（最多3次，指数退避）

**关键代码**：
```java
// 生产者：提交任务到队列
biMessageProducer.sendMessage(String.valueOf(chartId), isVip);

// 消费者：异步处理
@RabbitListener(queues = "bi_vip_queue", concurrency = "5-10")
public void receiveVipMessage(String message) {
    biAsyncService.executeGenChart(Long.parseLong(message));
}
```

---

### 2. 多级缓存（Caffeine + Redis）

**问题**：频繁查询数据库，性能低下

**解决方案**：三级缓存架构
- **L1（Caffeine）**：本地缓存，20秒过期，最大1000条
- **L2（Redis）**：分布式缓存，15分钟过期
- **L3（MySQL）**：数据库

**缓存策略**：

| 数据类型 | 缓存层级 | Key格式 | TTL | 说明 |
|---------|---------|---------|-----|------|
| 图表详情 | Redis + DB | `bi:chart:{id}` | 15分钟 | 不用Caffeine避免SSE冲突 |
| 图表列表 | Caffeine + Redis + DB | `chart:list:my:{userId}:v{version}` | 20秒/60秒 | 版本号机制 |
| 数据预览 | Caffeine + Redis + DB | `chart:data:{id}:p{page}` | 2分钟/10分钟 | 仅缓存前3页 |

**版本号机制**：
```java
// 删除缓存时，递增版本号而不是删除数据
String versionKey = "bi:chart:list:my:" + userId + ":v";
stringRedisTemplate.opsForValue().increment(versionKey);
```

---

### 3. 分布式锁（Redisson）

**问题**：用户重复提交相同请求，浪费AI Token

**解决方案**：
- 使用 Redisson 分布式锁防止重复提交
- 基于 `userId + 文件内容 + 分析目标 + 图表类型` 生成唯一锁Key
- `waitTime=0`（快速失败）+ `leaseTime=10秒`（防死锁）

**锁的Key设计**：
```java
// 新建图表
String lockKey = "chart:gen:" + userId + ":" + md5(fileContent + goal + chartType);

// 编辑图表
String lockKey = "chart:edit:" + userId + ":" + md5(chartId + goal + chartType);

// 重试图表
String lockKey = "chart:retry:" + userId + ":" + chartId;
```

**核心逻辑**：
```java
distributedLockService.executeWithLock(lockKey, 0, 10, () -> {
    // 业务逻辑
    return response;
});
```

---

### 4. 限流策略（Redisson RRateLimiter）

**问题**：防止恶意攻击和资源滥用

**解决方案**：双重限流
- **频率限流**：每秒最多2次请求（防止恶意刷接口）
- **每日限流**：普通用户3次/天，VIP用户50次/天

**限流回退机制**：
```java
// 先限流（扣减次数）
redisLimiterManager.doDailyLimit(userId, userRole);

try {
    // 尝试获取锁
    executeWithLock(...);
} catch (BusinessException e) {
    // 重复提交 → 回退限流次数
    if (e.getMessage().contains("重复提交")) {
        redisLimiterManager.rollbackDailyLimit(userId, userRole);
    }
}
```

---

## 接口功能总览

| 接口 | 功能 | 技术点 |
|------|------|--------|
| `/gen/async/rabbitmq` | 新建图表分析 | 异步化 + 分布式锁 + 限流 + 缓存删除 |
| `/edit/rabbitmq` | 编辑图表 | 分布式锁 + 限流回退 + 缓存删除 |
| `/gen/retry/rabbitmq` | 重试失败任务 | 分布式锁 + 限流回退 |
| `/get` | 获取图表详情 | Redis二级缓存 |
| `/my/list/page` | 我的图表列表 | 三级缓存 + 版本号机制 |
| `/data/preview` | 数据预览 | 三级缓存（仅缓存前3页） |

---

## 核心流程

### 新建图表分析流程

```
用户上传文件
    ↓
参数校验（文件大小、格式）
    ↓
限流检查（频率 + 每日额度）
    ↓
生成文件哈希（MD5）
    ↓
尝试获取分布式锁
    ├─ 成功 → 保存数据库 → 发送MQ消息 → 返回成功
    └─ 失败 → 回退限流 → 返回"重复提交"
    ↓
RabbitMQ 异步处理
    ↓
调用AI生成图表（重试3次）
    ↓
更新数据库 + 删除缓存 + SSE通知前端
```

---

## 性能优化成果

| 优化项 | 优化前 | 优化后 | 提升 |
|--------|--------|--------|------|
| 图表列表查询 | 200ms | 5ms（Caffeine命中） | **40倍** |
| 图表详情查询 | 50ms | 2ms（Redis命中） | **25倍** |
| 重复提交拦截 | 无 | 100%拦截 | **节省AI成本** |
| 并发处理能力 | 10 QPS | 100+ QPS | **10倍** |

---

## 面试重点

### 1. 为什么使用RabbitMQ而不是线程池？

**答**：
- **解耦**：Controller不需要关心任务执行细节
- **削峰**：消息队列可以缓冲流量高峰
- **可靠性**：消息持久化 + 死信队列保证不丢失
- **扩展性**：可以水平扩展消费者节点
- **优先级**：VIP队列优先处理，提升用户体验

### 2. 为什么图表详情不用Caffeine缓存？

**答**：避免与SSE实时通知冲突。
- AI分析完成后通过SSE推送通知
- 如果使用Caffeine，可能读到旧数据
- Redis是集中式缓存，更新后所有节点立即可见

### 3. 分布式锁的waitTime为什么设置为0？

**答**：快速失败原则。
- 重复提交场景下，等待是没有意义的
- 立即返回错误，用户体验更好
- 避免线程阻塞，提高系统吞吐量

### 4. 限流回退机制的必要性？

**答**：保护用户配额不被浪费。
- 先限流后加锁，如果锁获取失败需要回退
- 否则用户点两次按钮，配额被扣2次但只执行1次
- 体现了对用户的公平性

### 5. 缓存一致性如何保证？

**答**：
- **写操作**：先更新DB，再删除缓存（Cache Aside模式）
- **版本号机制**：列表缓存使用版本号，避免删除困难
- **TTL兜底**：所有缓存都设置过期时间
- **随机偏移**：TTL加随机偏移，避免缓存雪崩

---

## 技术难点与解决

### 难点1：缓存穿透
**问题**：恶意查询不存在的图表ID  
**解决**：布隆过滤器 + 空值缓存

### 难点2：缓存雪崩
**问题**：大量缓存同时过期  
**解决**：TTL加随机偏移（±10秒）

### 难点3：消息重复消费
**问题**：网络抖动导致重复消费  
**解决**：幂等性设计（检查图表状态）

### 难点4：分布式锁死锁
**问题**：线程异常崩溃，锁未释放  
**解决**：leaseTime自动过期 + finally释放

---

## 项目亮点总结

1. **异步化**：RabbitMQ + 死信队列 + 优先级队列
2. **多级缓存**：Caffeine + Redis + 版本号机制
3. **分布式锁**：Redisson + 快速失败 + 防死锁
4. **限流策略**：频率限流 + 每日限流 + 限流回退
5. **高可用**：消息重试 + 幂等性设计 + 缓存兜底
6. **性能优化**：查询性能提升40倍，并发能力提升10倍

---

**建议面试准备**：
- 熟练画出系统架构图和核心流程图
- 能够解释每个技术选型的原因
- 准备好性能优化的数据对比
- 思考可能的优化方向（如：引入Kafka、分库分表等）

