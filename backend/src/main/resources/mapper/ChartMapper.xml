<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yupi.springbootinit.mapper.ChartMapper">

    <resultMap id="BaseResultMap" type="com.yupi.springbootinit.model.entity.Chart">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="name" column="name" jdbcType="VARCHAR"/>
            <result property="goal" column="goal" jdbcType="VARCHAR"/>
            <result property="chartData" column="chartData" jdbcType="VARCHAR"/>
            <result property="chartType" column="chartType" jdbcType="VARCHAR"/>
            <result property="genChart" column="genChart" jdbcType="VARCHAR"/>
            <result property="genResult" column="genResult" jdbcType="VARCHAR"/>
            <result property="userId" column="userId" jdbcType="BIGINT"/>
            <result property="createTime" column="createTime" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="updateTime" jdbcType="TIMESTAMP"/>
            <result property="isDelete" column="isDelete" jdbcType="TINYINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,name,goal,chartData,
        chartType,genChart,genResult,userId,
        createTime,updateTime,isDelete
    </sql>

    <update id="createChartTable">
        CREATE TABLE ${tableName}
        (
        `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
        <foreach collection="colNames" item="col" separator=",">
            `${col}` TEXT NULL
        </foreach>,
        PRIMARY KEY (`id`)
        ) COMMENT '用户上传数据分表' COLLATE = utf8mb4_unicode_ci;
    </update>


    <insert id="insertChartData">
        INSERT INTO ${tableName}
        <foreach collection="colNames" item="col" open="(" separator="," close=")">
            `${col}`
        </foreach>
        VALUES
        <foreach collection="dataList" item="row" separator=",">
            (
            <foreach collection="row" item="val" separator=",">
                #{val}
            </foreach>
            )
        </foreach>
    </insert>

    <select id="queryChartData" resultType="java.util.LinkedHashMap">
        SELECT * FROM ${tableName}
    </select>


    <select id="queryChartDataByColNames" resultType="java.util.Map">
        select
        <foreach collection="colNames" item="col" separator=",">
            ${col}
        </foreach>
        from ${tableName}
    </select>


</mapper>
